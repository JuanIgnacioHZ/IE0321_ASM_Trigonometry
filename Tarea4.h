// Prototypes  for Tarea4.c file

int factorial(int n);				// Computes the factorial of the given int
float n_pow(float x, int n);			// Computes x^(n)
float seno(float equis, int taylor_iter);	// Computes the sine of the given angle using Taylor series
float coseno(float equis, int taylor_iter);	// Computes the cosine of the given angle using Taylor series
void trigo_equis(float equis);			// Calls all the trigonometric functions and prints it to terminal
int main();
